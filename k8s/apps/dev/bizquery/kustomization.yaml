# Note: the `bizquery` namespace AND `bizquery-secrets` are provisioned by Terraform (the argocd
# stack) so the Secret exists before pods start — see terraform/stacks/argocd/bizquery-secrets.tf.
# The ghcr-pull imagePullSecret is still created out-of-band (see README.md).
resources:
  - configmap.yaml
  - raw-files-pvc.yaml
  - postgres.yaml
  - api.yaml
  - worker.yaml
  - ingress.yaml
