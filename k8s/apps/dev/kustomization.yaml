resources:
  # Each app's manifests live in its own deployment repo; these Application CRs point Argo CD at them.
  # The app-of-apps deploys the CRs, which then sync each app. (loading-page replaced by the website.)
  - bizquery-app.yaml
  - website-app.yaml
