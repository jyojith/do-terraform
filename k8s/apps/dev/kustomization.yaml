resources:
  - loading-page/
  # bizquery manifests now live in the bizquery-platform-deployment repo. This Application CR points
  # Argo CD at that repo; the app-of-apps deploys the CR, which then syncs the app.
  - bizquery-app.yaml
